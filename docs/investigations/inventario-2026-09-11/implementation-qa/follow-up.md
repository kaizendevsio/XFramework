# Inventario acceptance follow-up — 2026-09-11

Parent acceptance owner: task `01a08ff2-8ac8-7b61-9793-036f75bd9b83`, report in the `2f19` worktree at `docs/investigations/inventario-2026-09-11/retest.md`. This evidence supports the follow-up to PR #446; it does not declare deployed acceptance of the new changes.

## Confirmed defects and changes

- Receiving a mixed base/variant PO with a new lot's calendar expiry failed PostgreSQL `timestamptz` persistence: the request carried `DateTimeKind.Unspecified`. Parent verified complete rollback before pausing writes. Purchasing, receiving, lot maintenance, and reservations now normalize incoming dates with the existing UTC helper. Portal calendar selections retain their selected day at UTC midnight. The receiving database-save error has specific guidance.
- Unopened selects displayed `all`, `base`, or a GUID and had no accessible name. Actual BlazorBlueprint 3.16 static rendering confirmed that the form select wrapper did not forward the label. `InventorySelect` composes the existing select primitives with an associated label and explicit selected text.
- `XfEntityPicker` rendered nested native buttons. It now uses the popover's single trigger button with the existing label, style, and disabled state. This is shared Portal markup; its enabled/disabled rendering and keyboard interaction were checked.
- At 390×844, receipt body width exceeded its available width (332 versus 308 pixels); Escape returned focus to BODY from receiving and mobile navigation. Shrinkable grid children fix the overflow without clipping. Programmatically opened receiving, PO, movement, and navigation overlays capture and restore their keyboard opener.
- Native template-column filtering returned no rows for a displayed product label. On live `d37c5fc3`, focus/Enter on the visible Balances Product filter, exact `E2E Shirt` input, blur, and focus/Enter on Apply produced one active filter and no results. The exact 3.16 library client grid calls `GetSortAndFilterValue`, whose template implementation reads `SortBy`, not `FilterBy`. Affected Inventario template columns now supply matching selectors.
- PO lines expose the existing unit field, defaulting to `each` and enforcing its 25-character limit. Category related products use tenant currency, concise quantities, and catalog status wording. Supplier detail has a back link; supplier-scoped orders show and clear that scope. Reports place controls/actions on one desktop row with mobile wrapping. Settings uses the supported `loader-circle` icon.

Library source used to diagnose filtering: [BbDataGrid client predicate](https://github.com/blazorblueprintui/ui/blob/538e23221372058b777b00988e866a9a9e18d42b/src/BlazorBlueprint.Components/Components/DataGrid/BbDataGrid.razor.cs), [template column raw value](https://github.com/blazorblueprintui/ui/blob/538e23221372058b777b00988e866a9a9e18d42b/src/BlazorBlueprint.Components/Components/DataGrid/BbDataGridTemplateColumn.razor.cs).

## Verification

- Real PostgreSQL wrapper suite: **39 passed**, including four calendar cases: PO expected date; mixed base 4 / variant 3 receipt with calendar lot expiry and exact replay producing one receiving document; lot metadata dates; reservation expiry and balance arithmetic. Three calendar paths were first reproduced failing before the backend fix.
- Portal nonbrowser tests: **91 passed**, including actual component rendering for picker enabled/disabled state and select display/accessibility, plus the library client-filter lookup value.
- Portal build: passed. API and integration test projects build through the wrapper test run. No migrations, dependencies, or deployment configuration changed.
- Isolated local browser harness used the actual modified components, BlazorBlueprint 3.16, Portal CSS, and focus script with synthetic data. Selecting the second product worked; Tab skipped the disabled picker; select showed `Size: Medium`; Escape restored focus to the keyboard opener for receiving/navigation. At 390×844 the receipt body had equal client/scroll width (298 pixels) and the 828-pixel dialog stayed inside the viewport.
- Local template-column Product Equals `Product 2` returned exactly one row (see `local-template-filter.png`). Report controls fit one row in a 900-pixel content area at 1280×800; card height was 200 pixels, versus the parent's approximately 320-pixel deployed card. At 390×844 controls wrapped into one column without horizontal overflow (`local-reports-*.png`). These are component/layout checks, not live backend workflow acceptance.
- Live read-only baseline: at 390×844 sidebar hiding, navigation opening, dialog bounds, vertical body scrolling, and keyboard reachability of Cancel passed. Focus return and receipt body width failed as described above. Screenshots `stock-390.png`, `navigation-390.png`, and `receiving-390*.png` capture the pre-fix deployment.

## Paging correction and evidence limitations

The preliminary Next clicks were inconclusive because hidden mounted tab panels exposed duplicate controls. On live `d37c5fc3`, targeting the visible Movements Next button, focusing it, and pressing Enter showed **21–34 of 34**. Balances → Movements retained page 2. Paging and retention therefore passed; all speculative filtered-Items caching was removed. `baseline-movements-preliminary.txt` is retained only as the original observation, not evidence of a defect. The Product filter failure was separately repeated with controlled keyboard activation and its entered value verified in the DOM.

The broad acceptance matrix remains owned by the parent. Parent owns all tenant fixture writes and will retry the rolled-back receipt after coordinated review/deployment, then verify dates, lot identity, stock math, reservations, report data, and live UI behavior. This task performed no additional live stock writes. Existing Water/Shirt history was not edited. Live baseline observations are from PR #446 (`7b4b66c2`) and subsequent Yap deployment `d37c5fc3`; local follow-up screenshots must not be presented as deployed results.
