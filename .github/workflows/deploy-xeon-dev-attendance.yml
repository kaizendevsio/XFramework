name: Deploy Xeon Dev - Attendance

on:
  push:
    branches: [develop]
    paths:
      - src/Modules/XFramework.Attendance/Attendance.Api/**
      - src/Modules/XFramework.Attendance/Attendance.Domain.Shared/**
      - src/Modules/XFramework.Attendance/Attendance.Integration/**
  workflow_dispatch:

permissions:
  contents: read

jobs:
  detect-shared-changes:
    name: Detect shared deploy ownership
    runs-on: ubuntu-latest
    outputs:
      has_shared: ${{ steps.shared.outputs.has_shared }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect shared changes
        id: shared
        uses: ./.github/actions/detect-shared-deploy-changes

  deploy:
    needs: detect-shared-changes
    if: needs.detect-shared-changes.outputs.has_shared != 'true'
    uses: ./.github/workflows/deploy-xeon-dev-service.yml
    with:
      service: attendance
      health_url: http://localhost:5182/health/ready
      diagnostics_services: bolt-hub attendance
    secrets: inherit
