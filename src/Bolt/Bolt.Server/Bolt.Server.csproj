<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <LangVersion>14</LangVersion>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>

        <PackageId>Bolt.Net.Server</PackageId>
        <Version>1.0.0-preview.1</Version>
        <Authors>KaizenDevs</Authors>
        <Description>Bolt — high-performance binary RPC + streaming hub server for ASP.NET Core. WebSocket middleware with zero-copy routing, connection pooling, and direct handler support. 47% faster than gRPC.</Description>
        <PackageTags>rpc;binary;server;hub;websocket;bolt;aspnetcore;high-performance</PackageTags>
        <RepositoryUrl>https://github.com/kaizendevsio/XFramework</RepositoryUrl>
        <PackageProjectUrl>https://github.com/kaizendevsio/XFramework</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageReadmeFile>BOLT.md</PackageReadmeFile>
    </PropertyGroup>

    <ItemGroup>
        <FrameworkReference Include="Microsoft.AspNetCore.App" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Bolt.Protocol\Bolt.Protocol.csproj" />
    </ItemGroup>

    <ItemGroup>
        <None Include="..\BOLT.md" Pack="true" PackagePath="\" />
    </ItemGroup>

</Project>
