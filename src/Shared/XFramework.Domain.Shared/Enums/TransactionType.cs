namespace XFramework.Domain.Shared.Enums;

public enum TransactionType
{
    Debit,  // Represents an outgoing transaction (money leaving the account)
    Credit  // Represents an incoming transaction (money entering the account)
}
