namespace Features.{EntityName}.{Action};

/// <summary>
/// Validator for {Action} {EntityName} request
/// Uses FluentValidation for request validation
/// </summary>
public class Validator : AbstractValidator<Request>
{
    public Validator()
    {
        // Example validation rules
        RuleFor(x => x.Name)
            .NotEmpty().WithMessage("Name is required")
            .MaximumLength(200).WithMessage("Name cannot exceed 200 characters");
        
        RuleFor(x => x.Email)
            .NotEmpty().WithMessage("Email is required")
            .EmailAddress().WithMessage("Invalid email format");
        
        RuleFor(x => x.Price)
            .GreaterThan(0).WithMessage("Price must be greater than 0");
        
        RuleFor(x => x.CategoryId)
            .NotEmpty().WithMessage("Category is required");
        
        // Complex validation with custom logic
        RuleFor(x => x)
            .Must(BeValidRequest)
            .WithMessage("Invalid request");
    }
    
    private bool BeValidRequest(Request request)
    {
        // Add custom validation logic here
        return true;
    }
}

// Instructions:
// 1. Replace {EntityName} with your entity name (e.g., Product)
// 2. Replace {Action} with your action name (e.g., Create, Update)
// 3. Add appropriate validation rules for your Request record
// 4. Delete example rules that don't apply
// 5. Delete these instructions

// Common validation rules:
// - NotEmpty() / NotNull()
// - MaximumLength(n) / MinimumLength(n)
// - GreaterThan(n) / GreaterThanOrEqualTo(n)
// - LessThan(n) / LessThanOrEqualTo(n)
// - EmailAddress()
// - Must(predicate) - custom validation
// - When(condition, action) - conditional validation