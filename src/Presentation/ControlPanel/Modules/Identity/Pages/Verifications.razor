@using XFramework.Blazor.Entity.Constants
@page "/identity/verifications"


<SectionContent SectionName="@LayoutConstants.HeaderButton">
    <MudButton StartIcon="@Icons.Material.Filled.Add" Color="Color.Inherit" OnClick="ButtonAction">
        Add
    </MudButton>
</SectionContent>
<Skeleton
    Item="@List"
    SkeletonType="@XSkeletonType.Table">
    <MudDataGrid Items="@List" Filterable="false" SortMode="@SortMode.None" Groupable="false">
        <Columns>
            
            <PropertyColumn Property="x => x.Type" />
            <PropertyColumn Property="x => x.Status" />
            <PropertyColumn Property="x => x.RequestedAt" />
            <PropertyColumn Property="x => x.VerifiedAt" />
            <TemplateColumn>
                <CellTemplate>
                    <MudButton Size="@Size.Small" Variant="@Variant.Filled" Color="@Color.Primary">Details</MudButton>
                </CellTemplate>
            </TemplateColumn>
        </Columns>
    </MudDataGrid>
</Skeleton>
